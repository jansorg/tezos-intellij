parameter string;
return string;
storage unit;
code { EMPTY_SET; # all commands expect a single type argument, which is missing here
    LEFT;
    NIL;
    NONE;
    RIGHT;

    EMPTY_SET DROP; # all commands expect a single type argument, but a an instruction is provided
    LEFT DROP;
    NIL DROP;
    NONE DROP;
    RIGHT DROP;

    EMPTY_SET (Pair int int); # all commands expect a single type argument, but data is passed as argument
    LEFT (Pair int int);
    NIL (Pair int int);
    NONE (Pair int int);
    RIGHT (Pair int int);

    EMPTY_SET { DROP; }; # all commands expect a single type argument, but an instruction block is passed as argument
    LEFT { DROP; };
    NIL { DROP; };
    NONE { DROP; };
    RIGHT { DROP; };
};
